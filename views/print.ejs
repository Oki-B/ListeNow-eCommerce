<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Product Invoice</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            font-family: 'Audiowide', cursive;
            background: linear-gradient(90deg, #ff7e5f, #feb47b);
            color: white;
            padding: 15px;
            border-radius: 5px;
        }
        .invoice-details {
            margin-top: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
            background: #fafafa;
        }
        table, th, td {
            border: 1px solid #ddd;
            text-align: left;
            padding: 10px;
        }
        th {
            background: linear-gradient(90deg, #ff7e5f, #feb47b);
            color: white;
        }
        .total {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
            font-weight: bold;
            font-size: 18px;
            color: #ff7e5f;
        }
        .store-info {
            display: flex;
            align-items: center;
            margin-top: 20px;
            padding: 15px;
            background: #f9f9f9;
            border-radius: 5px;
        }
        .store-info img {
            width: 100px;
            height: 100px;
            border-radius: 10px;
            margin-right: 15px;
        }
        .email-footer {
            margin-top: 20px;
            text-align: center;
            font-size: 14px;
            color: #555;
            padding: 10px;
            background: #f3f3f3;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>ListeNow</h1>
        <div class="invoice-details">
            <h2>Invoice</h2>
            <p>Thanks for your order!</p>
            <p>Hi, <strong><%= user.Store.storeName %></strong>! Here's what you purchased:</p>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><%= product.productName %></td>
                    <td>2</td>
                    <td>$<%= product.productPrice %></td>
                </tr>
            </tbody>
        </table>
        <div class="total">
            <p>Total:</p>
            <p>$<%= product.productPrice * 2 %></p>
        </div>
        <div class="store-info">
            <img src="<%= product.Store.profileUrl %>" alt="Store Logo">
            <div>
                <p><strong>Store Details:</strong></p>
                <p><%= product.Store.storeName %></p>
                <p><%= product.Store.location %></p>
            </div>
        </div>
        <div class="email-footer">
            <p>This invoice was sent to <%= user.email %></p>
        </div>
    </div>
</body>
</html>
